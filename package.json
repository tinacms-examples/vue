{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
 "scripts": {
  "dev": "tinacms dev -c \"nuxt dev\"",
  "build": "tinacms build && nuxt build",
  "generate": "tinacms build && nuxt generate",
  "preview": "tinacms build && nuxt preview",
  "postinstall": "tinacms build && nuxt prepare"
},
  "dependencies": {
    "date-fns": "^4.1.0",
    "nuxt": "^3.15.4",
    "tinacms": "^2.6.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.13.1",
    "@tailwindcss/typography": "^0.5.16",
    "@tinacms/cli": "^1.8.4",
    "@types/node": "^22.13.4"
  }
}
